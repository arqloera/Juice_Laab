<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>D_OBRA | Reporte Semanal</title>
    <script src="https://unpkg.com/scrollreveal"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <style>
        :root {
            --black: #000000;
            --dark-gray: #1a1a1a;
            --card-bg: #242424;
            --accent-blue: #0099ff;
            --whatsapp-green: #25D366;
            --text-white: #ffffff;
            --text-gray: #a0a0a0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: #121212;
            color: var(--text-white);
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .app-wrapper { width: 100%; max-width: 400px; padding-bottom: 120px; }

        .top-bar {
            background-color: var(--black);
            width: 100%;
            padding: 20px 0;
            display: flex;
            justify-content: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo-text { font-size: 1.5rem; font-weight: 800; letter-spacing: 4px; }

        header { padding: 30px 20px; }
        header h1 { font-size: 1.8rem; margin: 0; }
        header p { color: var(--text-gray); font-size: 0.9rem; margin: 5px 0; }

        .card {
            background-color: var(--card-bg);
            margin: 0 15px 20px 15px;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .card-title { font-size: 1.1rem; font-weight: 600; margin-bottom: 20px; }

        .stats-container { display: flex; justify-content: space-around; margin-bottom: 20px; }

        .progress-ring {
            width: 90px; height: 90px; border-radius: 50%;
            border: 5px solid #333; display: flex; align-items: center;
            justify-content: center; font-size: 1.2rem; font-weight: bold;
        }

        .ring-blue { border-color: var(--accent-blue); }

        .diff-badge {
            background-color: var(--black); padding: 10px; border-radius: 8px;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.85rem; color: var(--accent-blue); border: 1px solid #333;
        }

        .photo-strip { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
        .photo-thumb { aspect-ratio: 1/1; background: #333; border-radius: 4px; cursor: pointer; overflow: hidden; }
        .photo-thumb img { width: 100%; height: 100%; object-fit: cover; }

        /* Contact Section */
        .contact-btn {
            background-color: var(--accent-blue);
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 15px;
            border-radius: 10px;
            font-weight: bold;
            margin-top: 10px;
            gap: 10px;
        }

        /* Floating WhatsApp Button */
        .whatsapp-fab {
            position: fixed;
            bottom: 30px;
            right: 20px;
            background-color: var(--whatsapp-green);
            color: white;
            width: 55px;
            height: 55px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            z-index: 1000;
            text-decoration: none;
        }

        #overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95); z-index: 2000; justify-content: center; align-items: center;
        }
        #overlay img { max-width: 90%; max-height: 80%; border: 2px solid var(--accent-blue); }

        .footer-progress {
            position: fixed; bottom: 0; width: 100%; max-width: 400px;
            background: #121212; padding: 15px 20px; z-index: 99;
        }
        .bar-bg { height: 6px; background: #333; border-radius: 10px; overflow: hidden; }
        .bar-fill { height: 100%; background: var(--accent-blue); width: 65%; }

    </style>
</head>
<body>

<a href="https://wa.me/YOUR_PHONE_NUMBER" class="whatsapp-fab" target="_blank">
    <i class="ph ph-whatsapp-logo"></i>
</a>

<div id="overlay" onclick="this.style.display='none'">
    <img id="fullImg" src="">
</div>

<div class="app-wrapper">
    <div class="top-bar">
        <div class="logo-text">D_OBRA</div>
    </div>

    <header class="reveal">
        <h1>Reporte Semanal</h1>
        <p>Semana #12 - Proyecto Residencial</p>
    </header>

    <div class="card reveal">
        <div class="stats-container">
            <div class="stat-item"><div class="progress-ring ring-blue">75%</div></div>
            <div class="stat-item"><div class="progress-ring">80%</div></div>
        </div>
        <div class="diff-badge">⚠️ &nbsp; -3 Días de Diferencia</div>
    </div>

    <div class="card reveal">
        <div class="card-title">Avance de Obra</div>
        <p style="color: var(--text-gray); font-size: 0.85rem;">Cimentación de zona norte finalizada. Iniciando instalaciones hidráulicas.</p>
        <div class="photo-strip">
            <div class="photo-thumb" onclick="openImg('https://via.placeholder.com/600x600?text=Foto+1')"><img src="https://via.placeholder.com/150?text=1"></div>
            <div class="photo-thumb" onclick="openImg('https://via.placeholder.com/600x600?text=Foto+2')"><img src="https://via.placeholder.com/150?text=2"></div>
            <div class="photo-thumb" onclick="openImg('https://via.placeholder.com/600x600?text=Foto+3')"><img src="https://via.placeholder.com/150?text=3"></div>
        </div>
    </div>

    <div class="card reveal">
        <div class="card-title">Gestión de Proyecto</div>
        <p style="font-size: 0.85rem; color: var(--text-gray);">Si tiene dudas sobre los avances o la ruta crítica, contacte a su Project Manager.</p>
        <a href="mailto:manager@d_obra.com" class="contact-btn">
            <i class="ph ph-envelope"></i> Enviar Correo
        </a>
    </div>
</div>

<div class="footer-progress">
    <div class="bar-bg"><div class="bar-fill"></div></div>
</div>

<script>
    ScrollReveal().reveal('.reveal', { 
        delay: 200, distance: '40px', origin: 'bottom', duration: 1000, opacity: 0, easing: 'ease'
    });

    function openImg(url) {
        document.getElementById('fullImg').src = url;
        document.getElementById('overlay').style.display = 'flex';
    }
</script>

</body>
</html>